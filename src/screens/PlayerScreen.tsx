import { SectionCard } from '../components/SectionCard';

export function PlayerScreen() {
  return (
    <SectionCard title="Player">
      Eine schlichte Flaeche fuer die kommende Wiedergabe.
    </SectionCard>
  );
}
